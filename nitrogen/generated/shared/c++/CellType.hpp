///
/// CellType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::xlsx {

  /**
   * An enum which can be represented as a JavaScript union (CellType).
   */
  enum class CellType {
    STRING      SWIFT_NAME(string) = 0,
    NUMBER      SWIFT_NAME(number) = 1,
    BOOLEAN      SWIFT_NAME(boolean) = 2,
    EMPTY      SWIFT_NAME(empty) = 3,
    DATE      SWIFT_NAME(date) = 4,
    ERROR      SWIFT_NAME(error) = 5,
    FORMULA      SWIFT_NAME(formula) = 6,
    BLANK      SWIFT_NAME(blank) = 7,
  } CLOSED_ENUM;

} // namespace margelo::nitro::xlsx

namespace margelo::nitro {

  // C++ CellType <> JS CellType (union)
  template <>
  struct JSIConverter<margelo::nitro::xlsx::CellType> final {
    static inline margelo::nitro::xlsx::CellType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("string"): return margelo::nitro::xlsx::CellType::STRING;
        case hashString("number"): return margelo::nitro::xlsx::CellType::NUMBER;
        case hashString("boolean"): return margelo::nitro::xlsx::CellType::BOOLEAN;
        case hashString("empty"): return margelo::nitro::xlsx::CellType::EMPTY;
        case hashString("date"): return margelo::nitro::xlsx::CellType::DATE;
        case hashString("error"): return margelo::nitro::xlsx::CellType::ERROR;
        case hashString("formula"): return margelo::nitro::xlsx::CellType::FORMULA;
        case hashString("blank"): return margelo::nitro::xlsx::CellType::BLANK;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum CellType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::xlsx::CellType arg) {
      switch (arg) {
        case margelo::nitro::xlsx::CellType::STRING: return JSIConverter<std::string>::toJSI(runtime, "string");
        case margelo::nitro::xlsx::CellType::NUMBER: return JSIConverter<std::string>::toJSI(runtime, "number");
        case margelo::nitro::xlsx::CellType::BOOLEAN: return JSIConverter<std::string>::toJSI(runtime, "boolean");
        case margelo::nitro::xlsx::CellType::EMPTY: return JSIConverter<std::string>::toJSI(runtime, "empty");
        case margelo::nitro::xlsx::CellType::DATE: return JSIConverter<std::string>::toJSI(runtime, "date");
        case margelo::nitro::xlsx::CellType::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        case margelo::nitro::xlsx::CellType::FORMULA: return JSIConverter<std::string>::toJSI(runtime, "formula");
        case margelo::nitro::xlsx::CellType::BLANK: return JSIConverter<std::string>::toJSI(runtime, "blank");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert CellType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("string"):
        case hashString("number"):
        case hashString("boolean"):
        case hashString("empty"):
        case hashString("date"):
        case hashString("error"):
        case hashString("formula"):
        case hashString("blank"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
